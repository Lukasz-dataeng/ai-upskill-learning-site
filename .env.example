# Copy to .env (gitignored) once answer-generation is wired up. Not read by
# anything yet — scripts/build.mjs and the migration script don't use these.
# See specs/site-generator/spec.md §7 "No answer-generation step yet".

# EPAM DIAL endpoint, e.g. https://<your-dial-instance>/openai/deployments/<deployment-id>/chat/completions
DIAL_ENDPOINT=
DIAL_API_KEY=
DIAL_DEPLOYMENT_ID=
